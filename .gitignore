node_modules/
dist/
.env
.env.*
!.env.example
*.log
data/
workspaces/
artifacts/
coverage/
.DS_Store
:memory:
docs/

# sample-app is its own git repository: the demo needs its commit history to
# show change detection working. Track it as a submodule, or drop its .git
# directory if you want it committed as plain files.
